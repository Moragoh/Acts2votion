<objective>
To the left of the date display, add a "hamburger" UI that will open up the navigation menu when tapped
<objective>

<description>
When the "hamburger" is clicked, it should bring up a side menu (follow standard app UI conventions when doing this. I don't want a separate menu to pop up but rather a "floating" temporary menu that the user can scroll on). The side menu show all the dates for devotionals the app currently has.

The date that the user is currently on should be highlighted (follow standard app UI conventions for this too)
When a user clicks on a date, it should bring to the view of that date. 
When the user clicks away while the menu is up, the menu should disappear to the left (follow standard app UI conventions for this too)
</description>